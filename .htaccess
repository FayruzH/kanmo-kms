<IfModule mod_rewrite.c>
    RewriteEngine On

    # Route all requests to the public directory when project root is the web root.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
